#!groovy

properties([
  // H allows predefined but random minute see https://en.wikipedia.org/wiki/Cron#Non-standard_characters
  pipelineTriggers([cron('H 05 * * *')])
])

@Library("Infrastructure@DTSPO-7198")

def type = "java"
def product = "toffee"
def component = "recipe-backend"

withNightlyPipeline(type, product, component) {
  env.TEST_URL = "http://localhost:8080"
  enableSecurityScanBackend()
}
